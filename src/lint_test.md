# Nested Code Admonition

`````{admonition} Click the button to reveal!
:class: dropdown

Example code:

```yaml

name: jupytext-changes

on:
  push

jobs:
  sync-jupytext:
    runs-on: ubuntu-latest
    steps:

    # Checkout
```
`````
